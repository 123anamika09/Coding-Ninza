import java.util.*;
public class Solution {
    public static int[] getSecondOrderElements(int n, int []a) {
       Arrays.sort(a);
       n = a.length;
       int [] ans = new int[2];
       ans[0] = a[n-2];
       ans[1] = a[1];

    return ans;
    }
}
