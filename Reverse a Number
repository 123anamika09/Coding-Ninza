public class Solution {
    public static int reverseNumber(int n) {
        int res =0;
        while(n!=0) {
            int digits = n%10;
            res = res * 10 +digits;
            n = n/10;
        } 
        return res;
    }
}
